# -*- coding: utf-8 -*-  import os, sys  rs = r'D:\浏览器下载\简历建议.doc'  rp = r'D:\浏览器下载\简历--郑茜.pdf'  print("Suggestions exists:", os.path.exists(rs))  print("PDF exists:", os.path.exists(rp))  try:      from docx import Document      doc = Document(rs)      for p in doc.paragraphs:          if p.text.strip(): print(p.text)  except Exception as e:      print("Error:", e)  try:      from pypdf import PdfReader      reader = PdfReader(rp)      for p in reader.pages:          t = p.extract_text()          if t.strip(): print(t)  except Exception as e:      print("PDF Error:", e) 
