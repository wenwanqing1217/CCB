## 图表代码集合

本文档包含《基于微信小程序的校园盲盒即时配送平台设计与实现》一文中所有图表的PlantUML代码和draw.io提示词。

---

### 第1章 系统用例图（图1）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam actor {
  backgroundColor #FFEB3B
  borderColor #F9A825
}
skinparam useCase {
  backgroundColor #E3F2FD
  borderColor #1976D2
}
actor 用户 as U
actor 骑手 as R
actor 管理员 as A
rectangle "校园盲盒平台" {
  usecase "浏览盲盒" as UC1
  usecase "购买盲盒" as UC2
  usecase "发布盲盒" as UC3
  usecase "抽取盲盒" as UC4
  usecase "查看订单" as UC5
  usecase "抢单配送" as UC6
  usecase "确认送达" as UC7
  usecase "管理商品" as UC8
  usecase "管理用户" as UC9
}
U --> UC1
U --> UC2
U --> UC3
U --> UC4
U --> UC5
R --> UC6
R --> UC7
R --> UC5
A --> UC8
A --> UC9
A --> UC5
note right of UC1 : 用户浏览盲盒商品列表
@enduml
```

**draw.io提示词**：创建一个系统用例图。包含三个角色：用户、骑手、管理员。用例包括浏览盲盒、购买盲盒、发布盲盒、抽取盲盒、查看订单、抢单配送、确认送达、管理商品、管理用户。角色使用黄色背景(#FFEB3B)，用例使用浅蓝色背景(#E3F2FD)。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第2章 系统架构图（图2）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam shadowing false
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam rectangle {
  backgroundColor #E8F4FD
  borderColor #1E88E5
  borderWidth 1
}
skinparam database {
  backgroundColor #E3F2FD
  borderColor #1565C0
  borderWidth 1
}
rectangle "前端展示层" as Frontend {
  rectangle "首页" as Home
  rectangle "盲盒广场" as BlindBox
  rectangle "个人中心" as Profile
  rectangle "订单管理" as Order
  rectangle "社区互动" as Community
}
rectangle "业务逻辑层" as Logic {
  rectangle "用户认证服务" as Auth
  rectangle "盲盒服务" as BoxService
  rectangle "订单服务" as OrderService
  rectangle "推荐服务" as Recommend
  rectangle "通知服务" as Notify
  rectangle "配送服务" as Delivery
}
rectangle "数据持久层" as Data {
  database "用户数据库" as UserDB
  database "商品数据库" as BoxDB
  database "订单数据库" as OrderDB
  database "积分数据库" as CoinDB
}
Frontend -> Logic : API调用
Logic -> Data : 数据操作
note bottom of Frontend : 用户界面展示和交互
note bottom of Logic : 核心业务逻辑处理
note bottom of Data : 数据存储和管理
@enduml
```

**draw.io提示词**：创建一个三层架构图。分为前端展示层（浅蓝#E8F4FD）、业务逻辑层（浅蓝#E8F4FD）、数据持久层（浅青#E3F2FD）。前端层包含首页、盲盒广场、个人中心、订单管理、社区互动；业务逻辑层包含用户认证服务、盲盒服务、订单服务、推荐服务、通知服务、配送服务；数据层包含用户数据库、商品数据库、订单数据库、积分数据库。用箭头标注调用关系。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第3章 盲盒购买流程图（图3）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam activityBackgroundColor #E8F4FD
skinparam activityBorderColor #1E88E5
skinparam activityArrowColor #1E88E5
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam diamond {
  backgroundColor #FFF3E0
  borderColor #FF7043
}
start
:浏览盲盒商品;
:选择盲盒;
:查看详情;
if (确认购买?) then (是)
  :支付订单;
  if (支付成功?) then (是)
    :生成订单;
    :通知骑手;
    :骑手抢单;
    :骑手取货;
    :骑手配送;
    :确认收货;
  else (否)
    :支付失败;
  endif
else (否)
  :返回商品列表;
endif
stop
@enduml
```

**draw.io提示词**：创建一个盲盒购买流程图。使用活动图符号，活动节点背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)；判断节点背景为浅橙色(#FFF3E0)，边框橙色(#FF7043)；箭头为蓝色。流程包括：浏览商品→选择盲盒→查看详情→确认购买→支付订单→生成订单→通知骑手→骑手抢单→取货→配送→确认收货。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第4章 盲盒发布流程图（图4）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam activityBackgroundColor #E8F4FD
skinparam activityBorderColor #1E88E5
skinparam activityArrowColor #1E88E5
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam diamond {
  backgroundColor #FFF3E0
  borderColor #FF7043
}
start
:填写商品信息;
:上传商品图片;
:设置价格和分类;
:设置抽取概率;
if (信息完整?) then (是)
  :提交审核;
  if (审核通过?) then (是)
    :商品上架;
  else (否)
    :修改信息;
  endif
else (否)
  :补充信息;
endif
stop
@enduml
```

**draw.io提示词**：创建一个盲盒发布流程图。使用活动图符号，活动节点背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)；判断节点背景为浅橙色(#FFF3E0)，边框橙色(#FF7043)；箭头为蓝色。流程包括：填写商品信息→上传图片→设置价格分类→设置概率→信息完整检查→提交审核→审核通过→商品上架。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第5章 盲盒抽取流程图（图5）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam activityBackgroundColor #E8F4FD
skinparam activityBorderColor #1E88E5
skinparam activityArrowColor #1E88E5
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam diamond {
  backgroundColor #FFF3E0
  borderColor #FF7043
}
start
:进入抽取页面;
if (积分充足?) then (是)
  :扣除积分;
  :播放抽取动画;
  :执行概率算法;
  :显示抽取结果;
  :生成订单;
else (否)
  :提示积分不足;
  :跳转到积分充值;
endif
stop
@enduml
```

**draw.io提示词**：创建一个盲盒抽取流程图。使用活动图符号，活动节点背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)；判断节点背景为浅橙色(#FFF3E0)，边框橙色(#FF7043)；箭头为蓝色。流程包括：进入抽取页面→积分检查→扣除积分→播放动画→执行概率算法→显示结果→生成订单。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第6章 即时配送流程图（图6）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam activityBackgroundColor #E8F4FD
skinparam activityBorderColor #1E88E5
skinparam activityArrowColor #1E88E5
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam diamond {
  backgroundColor #FFF3E0
  borderColor #FF7043
}
start
:订单创建成功;
:推送订单信息给骑手;
:骑手抢单;
if (抢单成功?) then (是)
  :骑手前往取货点;
  :确认取货;
  :骑手配送;
  :确认送达;
  :订单完成;
else (否)
  :继续等待骑手;
endif
stop
@enduml
```

**draw.io提示词**：创建一个即时配送流程图。使用活动图符号，活动节点背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)；判断节点背景为浅橙色(#FFF3E0)，边框橙色(#FF7043)；箭头为蓝色。流程包括：订单创建→推送骑手→骑手抢单→前往取货→确认取货→配送→确认送达→订单完成。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第7章 订单状态流转图（图7）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam state {
  backgroundColor #E8F4FD
  borderColor #1E88E5
}
skinparam arrow {
  color #1E88E5
}
[*] --> 待付款
待付款 --> 待发货 : 支付成功
待付款 --> 已取消 : 用户取消
待发货 --> 待抢单 : 商家确认
待抢单 --> 配送中 : 骑手抢单
配送中 --> 已送达 : 骑手确认送达
已送达 --> 已完成 : 用户确认收货
已送达 --> 已评价 : 用户评价
[*] --> 已取消
@enduml
```

**draw.io提示词**：创建一个订单状态流转图。使用状态图符号，状态节点背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)；箭头为蓝色。状态包括：待付款、待发货、待抢单、配送中、已送达、已完成、已评价、已取消。标注状态转换条件。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第8章 社区互动流程图（图8）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam activityBackgroundColor #E8F4FD
skinparam activityBorderColor #1E88E5
skinparam activityArrowColor #1E88E5
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam diamond {
  backgroundColor #FFF3E0
  borderColor #FF7043
}
start
:进入社区页面;
if (选择功能?) then (捐赠)
  :填写捐赠信息;
  :提交捐赠;
  :获得爱心值奖励;
else (交换)
  :发布交换需求;
  :设置期望物品;
  :等待对方响应;
  if (对方同意?) then (是)
    :完成交换;
  else (否)
    :继续等待或取消;
  endif
endif
stop
@enduml
```

**draw.io提示词**：创建一个社区互动流程图。使用活动图符号，活动节点背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)；判断节点背景为浅橙色(#FFF3E0)，边框橙色(#FF7043)；箭头为蓝色。流程包括：进入社区→选择功能（捐赠/交换）→捐赠流程（填写信息→提交→获爱心值）；交换流程（发布需求→设置期望→等待响应→完成交换）。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第9章 基于内容的推荐流程图（图9）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam activityBackgroundColor #E8F4FD
skinparam activityBorderColor #1E88E5
skinparam activityArrowColor #1E88E5
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam diamond {
  backgroundColor #FFF3E0
  borderColor #FF7043
}
start
:用户浏览/购买商品;
:收集行为数据;
:分析偏好分类;
:分析价格区间;
:构建用户兴趣向量;
:计算商品相似度;
:从商品库筛选相似商品;
:过滤已浏览商品;
:排序推荐结果;
:展示推荐列表;
stop
@enduml
```

**draw.io提示词**：创建一个基于内容的推荐流程图。使用活动图符号，活动节点背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)；箭头为蓝色。流程包括：用户行为→收集数据→分析偏好→分析价格→构建用户兴趣向量→计算商品相似度→筛选相似商品→过滤已浏览→排序结果→展示推荐。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第10章 E-R图（图10）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam entity {
  backgroundColor #E8F5E9
  borderColor #388E3C
  borderWidth 1
}
skinparam relationship {
  lineColor #5D4037
}
entity "用户" as User {
  *_id: String <<PK>>
  *openid: String
  nickName: String
  avatarUrl: String
  role: String
  blindBoxCoins: Number
  lovePoints: Number
  createdAt: Date
  updatedAt: Date
}
entity "盲盒商品" as Box {
  *_id: String <<PK>>
  title: String
  images: Array
  price: Number
  category: String
  status: String
  openid: String <<FK>>
  createdAt: Date
  updatedAt: Date
}
entity "订单" as Order {
  *_id: String <<PK>>
  boxId: String <<FK>>
  buyerId: String <<FK>>
  sellerId: String <<FK>>
  riderId: String <<FK>>
  price: Number
  status: String
  createdAt: Date
  updatedAt: Date
}
entity "骑手" as Rider {
  *_id: String <<PK>>
  userId: String <<FK>>
  level: Number
  earnings: Number
  status: String
}
entity "积分记录" as CoinLog {
  *_id: String <<PK>>
  userId: String <<FK>>
  type: String
  amount: Number
  description: String
  createdAt: Date
}
User ||--o{ Box : "发布"
User ||--o{ Order : "购买/销售"
User ||--o{ CoinLog : "产生"
User ||--|| Rider : "成为"
Box ||--o{ Order : "被购买"
Rider ||--o{ Order : "配送"
@enduml
```

**draw.io提示词**：创建一个E-R图，包含五个实体：用户、盲盒商品、订单、骑手、积分记录。实体使用矩形，背景为浅绿色(#E8F5E9)，边框为深绿色(#388E3C)。显示各实体的属性和主键(PK)、外键(FK)。使用连线表示实体间关系：用户发布盲盒商品(一对多)、用户购买销售订单(一对多)、用户产生积分记录(一对多)、用户成为骑手(一对一)、盲盒商品被购买(一对多)、骑手配送订单(一对多)。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第11章 属性图（图11）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam node {
  backgroundColor #E3F2FD
  borderColor #1976D2
  borderWidth 1
}
skinparam edge {
  lineColor #5D4037
  arrowHeadColor #5D4037
}
node "用户" as User {
  _id: String
  nickName: String
  role: String
  blindBoxCoins: Number
  lovePoints: Number
}
node "盲盒商品" as Box {
  _id: String
  title: String
  price: Number
  category: String
  status: String
}
node "订单" as Order {
  _id: String
  price: Number
  status: String
  createdAt: Date
}
node "骑手" as Rider {
  _id: String
  level: Number
  earnings: Number
  status: String
}
node "积分记录" as CoinLog {
  _id: String
  type: String
  amount: Number
  createdAt: Date
}
User --> Box : 发布
User --> Order : 购买
User --> CoinLog : 产生
User --> Rider : 成为
Box --> Order : 被购买
Rider --> Order : 配送
@enduml
```

**draw.io提示词**：创建一个属性图，包含五个节点：用户、盲盒商品、订单、骑手、积分记录。节点使用圆角矩形，背景为浅蓝色(#E3F2FD)，边框为蓝色(#1976D2)。显示各节点的核心属性。边使用棕色(#5D4037)箭头表示关系：用户发布盲盒商品、用户购买订单、用户产生积分记录、用户成为骑手、盲盒商品被购买、骑手配送订单。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第12章 曼哈顿距离顺路匹配算法流程图（图12）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam activityBackgroundColor #E8F4FD
skinparam activityBorderColor #1E88E5
skinparam activityArrowColor #1E88E5
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam diamond {
  backgroundColor #FFF3E0
  borderColor #FF7043
}
start
:输入订单列表、骑手列表、道路网格信息;
:遍历待配送订单;
repeat :对每个订单
  :遍历骑手列表;
  repeat :对每个骑手
    :经纬度转平面坐标（乘转换因子）;
    :计算骑手到取货点曼哈顿距离;
    :计算取货点到送货点曼哈顿距离;
    :获取当前路段拥堵系数;
    :计算骑手-订单匹配评分;
  repeat while (还有骑手?)
  :按评分排序骑手;
  :选择最优骑手派单;
repeat while (还有订单?)
:输出派单结果;
stop
@enduml
```

**draw.io提示词**：创建一个曼哈顿距离顺路匹配算法流程图。使用活动图符号，活动节点背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)；判断节点背景为浅橙色(#FFF3E0)，边框橙色(#FF7043)；箭头为蓝色。流程包括：输入数据→遍历订单→遍历骑手→经纬度转换→计算曼哈顿距离→计算拥堵系数→计算匹配评分→排序骑手→选择最优派单。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第13章 基于内容的推荐算法流程图（图13）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam activityBackgroundColor #E8F4FD
skinparam activityBorderColor #1E88E5
skinparam activityArrowColor #1E88E5
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam diamond {
  backgroundColor #FFF3E0
  borderColor #FF7043
}
start
:获取用户历史浏览记录;
:提取浏览商品属性特征;
:构建商品属性向量;
:遍历候选商品集;
repeat :对每个候选商品
  :计算属性相似度;
repeat while (还有候选商品?)
:按相似度排序;
:输出推荐结果;
stop
@enduml
```

**draw.io提示词**：创建一个基于内容的推荐算法流程图。使用活动图符号，活动节点背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)；箭头为蓝色。流程包括：获取历史记录→提取商品属性→构建属性向量→遍历候选商品→计算相似度→排序→输出推荐。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第14章 模块调用关系图（图14）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam rectangle {
  backgroundColor #E8F4FD
  borderColor #1E88E5
}
skinparam database {
  backgroundColor #E3F2FD
  borderColor #0288D1
}
skinparam cloud {
  backgroundColor #FFF3E0
  borderColor #FF7043
}
rectangle "用户界面" as UI {
  rectangle "首页" as Home
  rectangle "盲盒列表" as BoxList
  rectangle "订单管理" as OrderUI
  rectangle "社区互动" as CommunityUI
}
cloud "云函数层" as Cloud {
  rectangle "userService" as UserSvc
  rectangle "boxService" as BoxSvc
  rectangle "orderService" as OrderSvc
  rectangle "deliveryService" as DeliverySvc
  rectangle "recommendService" as RecSvc
}
database "云数据库" as DB {
  rectangle "users" as Users
  rectangle "boxes" as Boxes
  rectangle "orders" as Orders
  rectangle "riders" as Riders
}
Home --> UserSvc : 登录/获取用户信息
BoxList --> BoxSvc : 获取盲盒列表
BoxList --> RecSvc : 获取推荐
OrderUI --> OrderSvc : 创建/查询订单
OrderUI --> DeliverySvc : 即时配送
CommunityUI --> UserSvc : 积分/爱心
UserSvc --> Users : CRUD
BoxSvc --> Boxes : CRUD
OrderSvc --> Orders : CRUD
DeliverySvc --> Riders : CRUD
DeliverySvc --> Orders : 更新状态
@enduml
```

**draw.io提示词**：创建一个模块调用关系图。分为三层：用户界面层（浅蓝#E8F4FD）、云函数层（浅橙#FFF3E0）、云数据库层（浅青色#E3F2FD）。界面层包含首页、盲盒列表、订单管理、社区互动；云函数层包含userService、boxService、orderService、deliveryService、recommendService；数据库层包含users、boxes、orders、riders表。用箭头标注调用关系。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第15章 性能优化架构图（图15）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam rectangle {
  backgroundColor #E8F4FD
  borderColor #1E88E5
}
skinparam cloud {
  backgroundColor #E3F2FD
  borderColor #0288D1
}
rectangle "性能优化" as Optimize {
  cloud "图片优化" as Image {
    rectangle "图片压缩"
    rectangle "WebP格式"
    rectangle "懒加载"
    rectangle "预加载"
  }
  cloud "数据缓存" as Cache {
    rectangle "LRU缓存"
    rectangle "本地存储"
    rectangle "请求缓存"
  }
  cloud "代码优化" as Code {
    rectangle "代码压缩"
    rectangle "分包加载"
    rectangle "按需引入"
  }
  cloud "渲染优化" as Render {
    rectangle "虚拟列表"
    rectangle "骨架屏"
    rectangle "节流防抖"
  }
}
Image --> Cache : 缓存优化后图片
Cache --> Code : 缓存数据减少请求
Code --> Render : 优化代码提升渲染
note bottom of Optimize : 全链路性能优化架构
@enduml
```

**draw.io提示词**：创建一个性能优化架构图。使用矩形和云朵符号，背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)。包含四大优化模块：图片优化（图片压缩、WebP格式、懒加载、预加载）、数据缓存（LRU缓存、本地存储、请求缓存）、代码优化（代码压缩、分包加载、按需引入）、渲染优化（虚拟列表、骨架屏、节流防抖）。模块间标注关联关系。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第16章 性能优化设计目标（图16）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
title 性能优化设计目标

bar {
  "首页加载时间" - ["优化前" as Before1] 3.0
  "首页加载时间" - ["设计目标" as After1] 1.5
  "图片加载时间" - ["优化前" as Before2] 1.2
  "图片加载时间" - ["设计目标" as After2] 0.4
  "接口响应时间" - ["优化前" as Before3] 0.8
  "接口响应时间" - ["设计目标" as After3] 0.3
}
note bottom of Before1 : 单位：秒
@enduml
```

**draw.io提示词**：创建一个性能优化对比柱状图。包含三组对比数据：首页加载时间（优化前3秒，目标1.5秒）、图片加载时间（优化前1.2秒，目标0.4秒）、接口响应时间（优化前0.8秒，目标0.3秒）。使用蓝色系配色，标注单位为秒。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 第3章 功能模块划分图（图3）

```plantuml
@startuml
skinparam backgroundColor #FFFFFF
skinparam handwritten false
skinparam defaultFontName "SimSun"
skinparam defaultFontSize 10
skinparam rectangle {
  backgroundColor #E8F4FD
  borderColor #1E88E5
  borderWidth 1
}
skinparam arrow {
  Color #1E88E5
}
rectangle "校园盲盒平台" as Platform {
  rectangle "首页模块" as Home
  rectangle "盲盒广场模块" as Market
  rectangle "盲盒抽取模块" as Draw
  rectangle "订单管理模块" as Order
  rectangle "即时配送模块" as Delivery
  rectangle "二手交换与捐赠模块" as Donation
}
Home --> Market : 跳转
Home --> Draw : 入口
Market --> Draw : 购买
Draw --> Order : 生成订单
Order --> Delivery : 配送请求
Delivery --> Order : 状态更新
Donation --> Order : 捐赠订单
note bottom of Platform : 六大核心功能模块
@enduml
```

**draw.io提示词**：创建一个功能模块划分图。使用矩形符号，背景为浅蓝色(#E8F4FD)，边框蓝色(#1E88E5)。包含六个模块：首页模块、盲盒广场模块、盲盒抽取模块、订单管理模块、即时配送模块、二手交换与捐赠模块。用箭头标注模块间的调用关系。整体风格简洁专业，适合学术论文使用，字体使用宋体，大小10号。

---

### 附录 图表编号对应表

| 图号 | 图表名称 | 对应章节 |
|-----|---------|---------|
| 图1 | 系统用例图 | 4.2.1 |
| 图2 | 系统架构图 | 4.2.2 |
| 图3 | 功能模块划分图 | 4.2 |
| 图4 | 盲盒购买业务流程图 | 4.3.1 |
| 图5 | 盲盒发布业务流程图 | 4.3.2 |
| 图6 | 即时配送流程图 | 4.3.3 |
| 图7 | 订单状态流转图 | 4.3.4 |
| 图8 | 智能推荐流程图 | 4.4.4 |
| 图9 | E-R图 | 4.5.1 |
| 图10 | 属性图 | 4.5.2 |
| 图11 | 曼哈顿距离顺路匹配算法流程图 | 4.6.1 |
| 图12 | 基于内容的推荐算法流程图 | 4.6.2 |
| 图13 | 模块调用关系图 | 5.4 |
| 图14 | 性能优化架构图 | 5.4 |
| 图15 | 首页界面截图 | 5.4.1 |
| 图16 | 盲盒发布界面 | 5.4.2 |
| 图17 | 盲盒抽取界面截图 | 5.4.3 |
| 图18 | 性能优化设计目标 | 6.3.4 |

### 附录 图表代码位置说明

| 图号 | 图表名称 | 代码位置（章节） |
|-----|---------|----------------|
| 图1 | 系统用例图 | 第1章 |
| 图2 | 系统架构图 | 第2章 |
| 图3 | 功能模块划分图 | 第3章 |
| 图4 | 盲盒购买业务流程图 | 第4章 |
| 图5 | 盲盒发布业务流程图 | 第5章 |
| 图6 | 即时配送流程图 | 第6章 |
| 图7 | 订单状态流转图 | 第7章 |
| 图8 | 智能推荐流程图 | 第8章 |
| 图9 | E-R图 | 第9章 |
| 图10 | 属性图 | 第10章 |
| 图11 | 曼哈顿距离顺路匹配算法流程图 | 第11章 |
| 图12 | 基于内容的推荐算法流程图 | 第12章 |
| 图13 | 模块调用关系图 | 第13章 |
| 图14 | 性能优化架构图 | 第14章 |
| 图15 | 首页界面截图 | 第15章 |
| 图16 | 盲盒发布界面 | 第16章 |
| 图17 | 盲盒抽取界面截图 | 第17章 |
| 图18 | 性能优化设计目标 | 第18章 |
